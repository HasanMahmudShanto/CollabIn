
@{
    ViewBag.Title = "Edit";
}


@model CollabIn.DTOs.ProjectDetailsDTO

@{
    ViewBag.Title = "Edit Project";
}

<h2>Edit Project</h2>

<div class="container my-5">
    @if (TempData["SuccessMsg"] != null)
    {
        <div class="alert alert-success rounded-3" role="alert">
            @TempData["SuccessMsg"]
        </div>
    }
    <form asp-action="ProjectEdit" method="post">
        <input type="hidden" name="Id" value="@Model.Id" />

        <!-- Header -->
        <div class="row mb-4">
            <div class="col">
                <h2 class="fw-bold">Edit Project: @Model.Title</h2>
            </div>
        </div>

        <!-- Project Details Card -->
        <div class="card shadow-sm border-0">
            <div class="card-body">

                <!-- Title -->
                <div class="mb-3">
                    <label for="Title" class="form-label">Project Title</label>
                    <input type="text" class="form-control" id="Title" name="Title" value="@Model.Title" required />
                </div>

                <!-- Status -->
                <div class="mb-3">
                    <label for="Status" class="form-label">Status</label>
                    <select class="form-select" id="Status" name="Status" required>
                        <option value="Open" @(Model.Status == "Open" ? "selected" : "")>Open</option>
                        <option value="Ongoing" @(Model.Status == "Ongoing" ? "selected" : "")>Ongoing</option>
                        <option value="Closed" @(Model.Status == "Closed" ? "selected" : "")>Closed</option>
                    </select>
                </div>

                <!-- Start and End Dates -->
                <div class="row mb-3">
                    <div class="col-md-6">
                        <label for="StartDate" class="form-label">Start Date</label>
                        <input type="datetime-local" class="form-control" id="StartDate" name="StartDate"
                               value="@Model.StartDate.ToString("yyyy-MM-ddTHH:mm")" required />
                    </div>
                    <div class="col-md-6">
                        <label for="EndDate" class="form-label">End Date</label>
                        <input type="datetime-local" class="form-control" id="EndDate" name="EndDate"
                               value="@Model.EndDate.ToString("yyyy-MM-ddTHH:mm")" required />
                    </div>
                </div>

                <!-- Description -->
                <div class="mb-3">
                    <label for="Details" class="form-label">Description</label>
                    <textarea class="form-control" id="Details" name="Details" rows="4" required>@Model.Details</textarea>
                </div>
                <label for="Members" class="form-label">Team Members</label>
                <!-- Team Members -->
                <ul class="list-group mb-2">
                    @for (int i = 0; i < Model.Name.Count; i++)
                    {
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <span>@Model.Name[i]</span>
                            @Html.ActionLink(
                                "Delete",
                                "RemoveMember",
                                "Supervisor",
                                new { ProjectId = Model.Id, MemberId = Model.MemberId[i] },
                                new
                                     {
                                    @class = "btn btn-sm btn-danger",
                                    onclick = $"return confirm('Are you sure you want to remove {Model.Name[i]}?');"
                                }
                            )
                        </li>
                    }
                </ul>


                <!-- Submit Button -->
                <div class="text-end">
                    <button type="submit" class="btn btn-primary">Save Changes</button>
                    <a href="@Url.Action("ProjectDetail", "Supervisor", new { Id = Model.Id })" class="btn btn-outline-secondary ms-2">Cancel</a>
                </div>

            </div>
        </div>
    </form>
</div>


